# Copy this file to .env and fill in your values.
# Never commit your actual .env file — it is gitignored.

# MongoDB connection string (Atlas URI)
# Get this from the Atlas dashboard → your cluster → Connect → Drivers
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/recip_ease?retryWrites=true&w=majority
